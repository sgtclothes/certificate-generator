<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Certificate</title>
</head>
<body class="w-full h-screen">
    <div class="container mx-auto">
        <div class="flex flex-col items-center mt-8 mb-8" id="title">
            <h1 class="text-4xl font-bold mb-3">Get Your Certificate</h1>
            <p class="bg-gray-200 p-2">/?name={Your Name}&image={Image Link}&top={px,rem, %}&left={px,rem, %}&right={px,rem, %}&bottom={px,rem, %}</p>
            <p>Jika source gambar dari Gdrive</p>
            <p class="text-red-400">"https://drive.google.com/uc?export=view&id=<span class="font-bold">ID-LINK_DRIVE</span>"</p>
        </div>
        <div class="flex justify-center w-full relative" id="certificate">
            <div class="name absolute top-[<%= queryTop %>] left-[<%= queryLeft %>] right-[<%= queryRight %>] bottom-[<%= queryBottom %>]">
                <h1 class="text-4xl font-bold text-slate-700"><%= queryName %></h1>
            </div>
            <% if(queryImage) { %>
                <div>
                    <img src="<%= queryImage %>" alt="Certificate">
                </div>
            <% } else { %>
                <div class="bg-gray-200 w-[50rem] h-[25rem] flex justify-center items-center text-3xl font-bold outline-dashed outline-2">
                    <h1>Certificate Template</h1>
                </div>
            <% } %>
        </div>
        <div class="w-full flex justify-center mt-5" id="button">
            <button class="bg-green-400 p-2" id="download-cert">Download Certificate</button>
        </div>
    </div>

    <script src="/js/script.js"></script>
</body>
</html>
